# Mobile service boundaries

Reserve this directory for typed backend clients and provider adapters. No network or permission requests run in the mobile preview. Privileged credentials must never enter the bundle. Add timeouts, cancellation, response validation, and safe error handling with each future integration.
