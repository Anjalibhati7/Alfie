# Sessions

Future explicit session lifecycle: idle, connecting, listening, thinking, speaking, interrupted, stopped, and error. Release microphone/location access at session end. No transcript persistence by default.

Documentation placeholder only; no integration implemented.
